import { useLocation } from "wouter";
import { ArrowLeft, Shield, Clock } from "lucide-react";
import { t } from "@/lib/i18n";
import BottomNav from "@/components/BottomNav";
export default function SecuritySettings(){const[,setLocation]=useLocation();return <div className="w-full max-w-[430px] mx-auto min-h-screen bg-background pb-20"><header className="sticky top-0 z-40 px-4 py-4 flex items-center gap-3" style={{background:"rgba(13,11,20,0.95)",backdropFilter:"blur(20px)",borderBottom:"1px solid rgba(255,255,255,0.06)"}}><button onClick={()=>setLocation("/settings")} data-testid="btn-back-security"><ArrowLeft size={22} className="text-white/80"/></button><h1 className="text-base font-semibold text-white">{t("security")}</h1></header><div className="px-4 py-4 flex flex-col gap-4"><div><p className="text-white/50 text-xs font-semibold uppercase tracking-wider mb-3">Authentication</p><div className="rounded-2xl p-4" style={{background:"rgba(255,255,255,0.04)",border:"1px solid rgba(255,255,255,0.07)"}}><div className="flex items-center gap-3"><div className="w-8 h-8 rounded-lg flex items-center justify-center" style={{background:"rgba(99,102,241,0.2)"}}><Shield size={16} className="text-indigo-400"/></div><div><p className="text-white/85 text-sm font-medium">Account authentication</p><p className="text-white/40 text-xs mt-0.5">Your Yuniko account uses the authenticated session system.</p></div></div></div></div><div><p className="text-white/50 text-xs font-semibold uppercase tracking-wider mb-3">Active Sessions</p><div className="rounded-2xl p-4" style={{background:"rgba(255,255,255,0.04)",border:"1px solid rgba(255,255,255,0.07)"}}><p className="text-white/70 text-sm">Detailed device sessions are not exposed yet.</p><p className="text-white/35 text-xs mt-1">Yuniko no longer displays fabricated devices or locations.</p></div></div><button className="flex items-center gap-3 px-4 py-4 rounded-2xl text-left" style={{background:"rgba(255,255,255,0.04)",border:"1px solid rgba(255,255,255,0.07)"}} disabled><Clock size={18} className="text-white/40"/><span className="flex-1 text-white/50 text-sm">{t("loginHistory")} — unavailable</span></button></div><BottomNav/></div>}
