# Production API used by the Yuniko Cloudflare Pages frontend.
# Keep this explicit so production builds never fall back to an empty/missing API URL.
VITE_API_URL=https://yuniko-api.lafatriniainaallane.workers.dev
